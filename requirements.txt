# Recommended full development/runtime environment for BeingBeyond D1 EDU SDK.
# Python: 3.8 is recommended for Isaac Gym compatibility and the cp38 SDK wheel.

numpy>=1.23,<2.0
pyserial>=3.5,<4
python-can>=4.3,<5
pin>=2.6,<3.0
cvxpy>=1.4,<1.7
matplotlib>=3.7,<3.10

# Build the Cython extension modules from source.
Cython>=3.0,<3.1
setuptools>=68
wheel>=0.42
build>=1.2

# Optional but useful for this repository.
opencv-python>=4.8,<5
mediapipe==0.10.10
meshcat>=0.3,<1
